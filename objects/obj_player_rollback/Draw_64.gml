state.on_draw_GUI();

////var selection = selections[i];
////var selection_spawn = selection.spawn_point;
//var _text = "";
////var text_x = x;
////var text_y = y - selection.vertical_margin;
	
//if (character_index >= 0)
//	_text = get_character_sprite_description(characters_list[character_index]);
	
//if (is_ready)
//	_text = "P" + string(player_id + 1) + " READY";
	
//prepare_text_draw(font, fa_center, fa_center);
//draw_outlined_text(x, y - vertical_margin, _text);

//var _hm = input.state.is_left_pressed ? "true" : "false";
////string_
////draw_outlined_text(x, y - vertical_margin - 50, _hm);
////draw_outlined_text(x, y - vertical_margin - 50, string(input.rollback_input[$ "gamepad_left_horizontal_axis"]));
////draw_outlined_text(x + 75, y - vertical_margin - 50, string(input.rollback_previous_input.gamepad_left_horizontal_axis));

////if (can_start) {
////	prepare_text_draw(font_start_match, fa_center, fa_center);
////	draw_outlined_text(gui_width * .5, gui_height * .5 + 280, "START MATCH");
////}